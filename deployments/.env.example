# SmokedMeat Kitchen Deployment Configuration
# Copy to .env and customize

# Required: Your domain name (must have DNS pointing to this server)
DOMAIN=kitchen.example.com

# Auth is enabled by default. Uncomment to disable for local testing only.
# AUTH_REQUIRED=false
